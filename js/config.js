'use strict';

var _const = {
    RED: 1,
    GREEN: 2,
    BLUE: 3,
    POSSIBLESHIFT: 3,
    REDPOSSIBLE: this.RED + this.POSSIBLESHIFT,
    GREENPOSSIBLE: this.GREEN + this.POSSIBLESHIFT,
    BLUEPOSSIBLE: this.BLUE + this.POSSIBLESHIFT,
    EMPTY: 0,
    NONBOARD: -1,
    NONEXISTENT: -2,
    SQRTTHREE: Math.sqrt(3),
    WIDTH: 400,
    HEIGHT: 400,
    BASE_NUMHEXES: 12,
    COMP_WAIT: 1000
};
